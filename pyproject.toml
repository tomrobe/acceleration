[build-system]
requires = ["setuptools>=64", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "acceleration"
dynamic = ["version"]

dependencies = [
    "numpy>=1.20.0",
    "scipy>=1.7.0",
]

[tool.setuptools_scm]
